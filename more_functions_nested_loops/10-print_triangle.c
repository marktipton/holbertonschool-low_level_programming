#include "main.h"

/**
 * print_triangle - prints a triangle using spaces and ###
 *
 * @size: size of triangle
 *
 * Return: Always 0.
 */
void print_triangle(int size)
{
int x, y;

if (size <= 0)
{
	_putchar('\n');
}
else 
{
	y = n;
	while ((n - y + 1) < n)
	{
		while (x > 0)
		{
			x = n - 1;
			_putchar(' '); 
			x = x - 1;
		}
		_putchar('#');
		y = y - 1;

	}
}
}
