#include "main.h"

/**
 * puts2 - prints every other character of a string
 *
 * Return: Always 0.
 */
void puts2(char *str)
{
    char *str;

    str = "0123456789";
    puts2(str);
    return (0);
}
