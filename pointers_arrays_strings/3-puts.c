#include "main.h"

/**
 * _puts - prints string and \n
 *
 * Return: Always 0.
 */
void _puts(char *str)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _puts(str);
    return (0);
}
