#include "main.h"
#include <stdio.h>

/**
 * is_prime_number - returns 1 if the input is a prime number
 *@n: number
 * Return: Always 0.
 */
int is_prime_number(int n)
{
	int i;

	i * i = n;

	if ()
	{
		return (1);
	}	
	return (0);
}
